# Copy the model lines into the project .env file to use an Ollama server that
# exposes its OpenAI-compatible endpoint. Replace the model with one installed
# locally (`ollama list`). No API key is sent.
EEVEETUBER_MODEL__PROVIDER=openai_compatible
EEVEETUBER_MODEL__BASE_URL=http://127.0.0.1:11434/v1
EEVEETUBER_MODEL__MODEL=replace-with-your-local-model

# Keep the foreground chat lane responsive on thinking models such as Qwen3.5.
# This sends the literal OpenAI-compatible value "none"; leaving the setting out
# omits the field and lets the model/server choose its default.
EEVEETUBER_MODEL__REASONING_EFFORT=none
EEVEETUBER_MODEL__MAX_OUTPUT_TOKENS=512

# Ollama does not provide the /audio/speech contract. Keep deterministic fake
# speech until a separate compatible TTS server is configured.
EEVEETUBER_SPEECH__PROVIDER=fake

# Ollama also does not provide /audio/transcriptions. The fake below lets the
# microphone/VAD/barge-in path run end to end with a deterministic transcript.
# Point these ASR settings at a separate Whisper-compatible server for real text.
EEVEETUBER_ASR__PROVIDER=fake
EEVEETUBER_VOICE__ENABLED=true
